---
description: Fetch the public automatic briefing data layer.
agent: opencode-trading
---
Use the `trading-briefing-fetch` skill with `data-analyst` for local validation. This is a user-triggered network action. Write only to an explicitly non-Vault path or the OpenCode output root. Validate the path with `safe-output`; never write the Obsidian Vault. Arguments: $ARGUMENTS
