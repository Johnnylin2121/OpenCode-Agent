---
description: Run the Amazon advertising and product analysis workflow.
agent: opencode-amazon
---
Use the `amazon-ad-analysis` skill with `data-analyst` for schema and KPI validation. Read source files without modifying them. Keep all Markdown, Excel, calibration drafts, and reports in OpenCode non-Vault storage. Validate paths with `safe-output`; do not access Amazon accounts or execute backend actions. Arguments: $ARGUMENTS
