---
description: Read trading memories and produce a non-Vault consolidation draft.
agent: opencode-trading
---
Use the `trading-memory-consolidate` skill with `document-reader` for read-only memory inspection. Classify conflicts and write a draft plus migration plan to OpenCode non-Vault storage. Never move, delete, or write Vault files. Validate output paths with `safe-output`. Arguments: $ARGUMENTS
