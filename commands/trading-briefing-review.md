---
description: Review a formal briefing using read-only inputs.
agent: opencode-trading
---
Use the `trading-briefing-review` skill with `document-reader` for formal inputs. Read the formal briefing and optional automatic data without modifying them. Return the review in the conversation or write only to OpenCode non-Vault storage. Arguments: $ARGUMENTS
