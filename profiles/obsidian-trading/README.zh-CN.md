# 可选知识库 Profile

[English](README.md) | 简体中文

本目录不包含 Vault 文件和用户数据。

Windows 或 macOS 可以在本机配置自己的知识库路径，例如：

```text
{VAULT_PATH}/
  trading/
  reviews/
  memory/
```

OpenCode 默认只读取用户批准的知识库文件。持久化产物放在 `{OPENCODE_OUTPUT_ROOT}`。任何知识库写入都需要明确任务授权、来源记录和变更范围说明。
